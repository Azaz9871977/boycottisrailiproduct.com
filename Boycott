<!DOCTYPE html>
<html lang="hi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>बॉयकॉट इज़राइल - पावर ऑफ़ पीपल</title>
    <style>
        /* बेसिक स्टाइल्स */
        :root {
            --primary: #FF6B6B;
            --secondary: #4ECDC4;
        }
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            background: #f8f9fa;
        }
        
        /* हीरो सेक्शन */
        .hero {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: white;
            padding: 4rem 1rem;
            text-align: center;
            position: relative;
            overflow: hidden;
        }
        .hero h1 {
            font-size: 2.5rem;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
        }
        /* गेमिफिकेशन बैज */
        .badges {
            display: flex;
            gap: 10px;
            justify-content: center;
            margin: 20px 0;
        }
        .badge {
            background: gold;
            padding: 8px 15px;
            border-radius: 20px;
            font-weight: bold;
            animation: pulse 2s infinite;
        }
        /* इमेज अपलोड सेक्शन */
        .scanner-section {
            max-width: 800px;
            margin: 2rem auto;
            padding: 20px;
            background: white;
            border-radius: 15px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }
        
        /* एनिमेशन */
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.1); }
            100% { transform: scale(1); }
        }
        .pulse { animation: pulse 2s infinite; }
        /* सोशल शेयर बटन */
        .share-buttons {
            display: flex;
            gap: 10px;
            justify-content: center;
            margin-top: 20px;
        }
        .share-btn {
            padding: 10px 20px;
            border-radius: 25px;
            border: none;
            cursor: pointer;
            transition: transform 0.3s;
        }
        .share-btn:hover {
            transform: translateY(-3px);
        }
    </style>
</head>
<body>
    <!-- हीरो सेक्शन -->
    <div class="hero">
        <h1>इज़राइली प्रोडक्ट्स का बायकॉट करें 🇵🇸</h1>
        <p>अपने फोन से स्कैन करें और जानें कौन-सा प्रोडक्ट बदलना है!</p>
        
        <!-- बैजेस -->
        <div class="badges">
            <div class="badge">🏆 टॉप स्कैनर</div>
            <div class="badge">🛡️ फ्रीडम फाइटर</div>
        </div>
    </div>
    <!-- स्कैनर सेक्शन -->
    <div class="scanner-section">
        <div>
            <input type="file" id="uploadInput" accept="image/*" hidden>
            <button class="share-btn" style="background: var(--primary);" 
                    onclick="document.getElementById('uploadInput').click()">
                📸 फोटो स्कैन करें
            </button>
            <button class="share-btn" style="background: var(--secondary);" 
                    onclick="captureImage()">
                🎥 लाइव कैमरा
            </button>
        </div>
        
        <img id="preview" src="#" alt="स्कैन इमेज" style="display: none; margin: 20px auto; max-width: 300px; border-radius: 10px;">
        
        <!-- रिजल्ट और शेयर सेक्शन -->
        <div id="result" style="padding: 15px; margin: 20px; border-radius: 10px;"></div>
        
        <div class="share-buttons">
            <button class="share-btn" style="background: #25D366;" onclick="shareOnWhatsApp()">
                WhatsApp पर शेयर करें
            </button>
            <button class="share-btn" style="background: #E1306C;" onclick="shareOnInstagram()">
                Instagram पर शेयर करें
            </button>
        </div>
    </div>
    <!-- लाइव काउंटर -->
    <div style="text-align: center; padding: 20px;">
        <h2>हमारा इम्पैक्ट</h2>
        <div id="impactCounter" style="font-size: 2.5rem; color: var(--primary);">1,00,000+</div>
        <small>बायकॉट किए गए प्रोडक्ट्स</small>
    </div>
    <!-- स्क्रिप्ट -->
    <script>
        // मॉक डेटा
        let userPoints = 0;
        let totalScans = 100000;
        // इमेज प्रोसेसिंग फंक्शन
        async function checkImage(imageFile) {
            const resultDiv = document.getElementById('result');
            
            // रैंडम रिजल्ट (डेमो के लिए)
            const isIsraeli = Math.random() > 0.5;
            
            // पॉइंट्स अपडेट
            userPoints += 10;
            totalScans += 1;
            document.getElementById('impactCounter').innerText = `${totalScans.toLocaleString()}+`;
            
            // रिजल्ट दिखाएं
            resultDiv.style.backgroundColor = isIsraeli ? '#fff3cd' : '#d4edda';
            resultDiv.innerHTML = isIsraeli 
                ? `⚠️ <b>यह इज़राइली प्रोडक्ट है!</b><br>10 पॉइंट्स कमाएं! (कुल पॉइंट्स: ${userPoints})`
                : '✅ यह प्रोडक्ट सेफ है!';
        }
        // सोशल शेयर फंक्शन
        function shareOnWhatsApp() {
            const message = encodeURIComponent(
                `मैंने ${userPoints} इज़राइली प्रोडक्ट्स बायकॉट किए! आप भी जुड़ें:\n${window.location.href}`
            );
            window.open(`https://wa.me/?text=${message}`, '_blank');
        }
        function shareOnInstagram() {
            alert('इंस्टाग्राम स्टोरी पर स्कैन रिजल्ट शेयर करें!');
        }
        // पिछला कैमरा कोड यहाँ जोड़ें
        // (पिछले उत्तर में दिया गया captureImage() फंक्शन)
    </script>
</body>
</html>
